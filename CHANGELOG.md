# سجل التغييرات — لوحة تحكم Qhub

كل تغيير يُنشر على `qhub-admin.apexes.click` يُسجَّل هنا، الأحدث أولاً.

---

## 2026-09-10 — ملخص تكلفة كامل في شاشة تأكيد الاشتراك

- **قُسمت الشاشة إلى بطاقتين**: «تفاصيل الاشتراك» (العميل، الباقتان، المدة، موعد التنفيذ) و**«ملخص التكلفة»** الجديدة، بدل بطاقة واحدة كانت تكتفي بسطر «السعر».
- **المقاصة معروضة بخطواتها** عند التبديل الفوري: سعر الباقة الحالية، سعر الباقة الجديدة، فرق السعر، المتبقي من الدورة، معادلة المقاصة، الفرق المستحق، الضريبة، ثم **الإجمالي المستحق الآن**. وإن كان الفرق سالباً يُعرض كـ«رصيد يُضاف للعميل».
- **المقاصة**: `(سعر الجديدة − سعر الحالية) × المتبقي ÷ طول الدورة` — بالأيام في الاشتراك الشهري وبالشهور في السنوي.
- **عند التخفيض المجدول** تُعرَض الحالة كما هي: المقاصة لا تنطبق، المستحق الآن صفر، ثم تفصيل الفاتورة المجدولة للفترة القادمة وإجماليها.
- **الضريبة صارت بخانتين عشريتين** بدل التقريب إلى عدد صحيح — كانت ضريبة 7 ر.ع تظهر صفراً. طُبِّق التصحيح في المخزن أيضاً حتى تطابق الفاتورة ما تعرضه الشاشة.

## 2026-09-10 — رقم هاتف اختياري للموظف

- **حقل «رقم الهاتف»** في نموذج إضافة/تعديل الموظف، موسوم بـ«(اختياري)» — الدعوة تُرسل على البريد، فالهاتف بيان تواصل لا شرط للإضافة.
- الحفظ يمرّ بلا رقم، وإن أُدخل رقم بصيغة غير صحيحة يُرفض برسالة واضحة. أُضيف `phone` اختيارياً إلى نوع الموظف.

## 2026-09-10 — زر رجوع في نموذج العميل داخل مسار الاشتراك

- **زر رجوع** بجانب عنوان «إضافة عميل جديد» حين يُفتح النموذج من داخل مسار إنشاء الاشتراك، يعيد إلى نموذج الاشتراك مباشرة. قبله لم يكن أمام المستخدم سوى «إلغاء» أو زر الإغلاق، وكلاهما لا يوضّح أن ما خلفه نموذج الاشتراك.
- الزر اختياري (`onBack`) فلا يظهر حين يُفتح النموذج من صفحة العملاء، إذ لا مكان يُرجَع إليه.

## 2026-09-10 — توحيد لغة النصوص على الفصحى

- أُعيدت صياغة نصوص مسار الاشتراك بالفصحى بعد أن كُتبت بالعامية المصرية: خيارا توقيت التبديل، وملاحظة التخفيض داخل النموذج، وشرح «ما الذي سيحدث؟» في شاشة التأكيد، وملاحظة حالة الاشتراك بعد الإنشاء.
- أُصلحت كذلك رسالة الرد خارج ساعات الدوام في إعدادات الودجت.

## 2026-09-10 — تبسيط مسار إنشاء الاشتراك

- **حُذفت نافذتان وسيطتان** من مسار إنشاء الاشتراك: «تخفيض/ترقية باقة العميل» (كانت تكرّر الملاحظة الظاهرة أصلاً داخل النموذج)، و«تبديل الباقة إلى …» بخيارات التوقيت. المسار صار خطوتين: النموذج ثم التأكيد.
- **توقيت التبديل صار قاعدة لا خياراً**: التخفيض يقع **عند انتهاء الاشتراك الحالي**، والترقية تقع فوراً مع احتساب فرق السعر. الملاحظة داخل النموذج تذكر ما سيحدث بدل أن تسأل عنه.
- **شاشة التأكيد تغطي التبديل**: تعرض الباقة الحالية والجديدة و«موعد التنفيذ»، وتشرح أن التخفيض يولّد فاتورة «مجدولة» يمكن إلغاؤها من «إلغاء الجدولة».
- خيارات التوقيت باقية كما هي في إجراء **«تبديل الباقة»** من قائمة صف الاشتراك — الحذف يخص مسار الإنشاء فقط.

## 2026-09-10 — وسم «مجدولة» في الاشتراكات

- **وسم «مجدولة»** بجانب وسم الحالة في جدول الاشتراكات لكل اشتراك عليه تبديل باقة مجدول، مع تلميح يذكر الباقة القادمة. قبله كانت الجدولة مذكورة كسطر صغير داخل عمود تاريخ البدء فقط، فيسهل أن تفوت.
- **خيار «مجدولة» في فلتر الحالات** لعزل الاشتراكات التي تنتظر تبديلاً — وهو ليس حالة مخزَّنة بل اختيار للاشتراكات التي تحمل `scheduledPlanId`.
- الوسم والفلتر يختفيان فور «إلغاء الجدولة» أو وقوع التبديل.

## 2026-09-10 — أعمدة المبالغ بالدولار + حالة فاتورة «مجدولة»

- **كل أعمدة المبلغ/الإجمالي في اللوحة صارت بالدولار**: جدول الفواتير في المالية، وجدولا الفواتير والمعاملات في صفحة العميل، وعمود المبلغ في الاشتراكات — مع إضافة `(USD)` إلى عناوين الأعمدة. التحويل يتم من عملة السجل عبر `formatUSD` الجديدة. تصدير CSV وتقرير PDF للفواتير بالدولار كذلك.
- **الفاتورة المطبوعة ونافذة عرض الفاتورة بقيتا بالعملة المحلية** لأنهما مستند موجّه للعميل لا تقرير إداري.
- **حالة فاتورة جديدة: «مجدولة»** — تُنشأ فاتورة للفترة القادمة بحالة مجدولة عند جدولة تنزيل الباقة (تبديل عند انتهاء الفترة الحالية)، وتبقى كذلك حتى يقع التبديل أو تُلغى الجدولة. تظهر في فلتر الحالات، ولا تُحتسب ضمن «إجمالي الفواتير» لأنها لم تُصدر بعد.
- **إجراء «إلغاء الجدولة»** في قائمة صف الاشتراك، يظهر فقط متى كان هناك تبديل مجدول، ويحذف الفاتورة المجدولة معه. التبديل الفوري يلغي أي جدولة سابقة وفاتورتها.

## 2026-09-10 — منصات التكامل صارت بيانات seeder

- **أُزيلت إدارة المنصات من اللوحة**: زر «إضافة منصة»، وإجراءا التعديل والحذف في الجدول، ونموذج الإضافة/التعديل بكامله (الاسم، الكود، النوع، الشعار، الدول، طرق الربط وخطواتها). المنصات تأتي من الباكند كبيانات seeder، مثلها مثل أنواعها.
- **ما بقي**: الجدول للعرض فقط مع البحث وفلتر الفئة، ومفتاح **التفعيل/الإيقاف** لكل منصة — فهذا إعداد تشغيلي لا بيانات seeder.
- عنوان الصفحة الفرعي صار «تفعيل وإيقاف منصات التكامل المتاحة — قائمة المنصات تأتي من النظام».

## 2026-09-10 — «عملاء في الفترة التجريبية» بدل «فترات تجريبية تنتهي قريباً»

- في **نظرة عامة**، حلّ قسم **«عملاء في الفترة التجريبية»** محلّ «فترات تجريبية تنتهي قريباً». كان القسم السابق يقتصر على من تبقّى لهم 7 أيام أو أقل، فيختفي منه معظم العملاء التجريبيين.
- القسم الجديد يعرض **كل العملاء في الفترة التجريبية** مرتّبين من الأقرب انتهاءً، والعدّاد في الترويسة يعكس إجماليهم لا عدد الصفوف المعروضة.
- عمود «المتبقي» صار يميّز الفترات المنتهية بوسم **«منتهية»**، ويعرض «—» لمن لا يحمل تاريخ انتهاء.

## 2026-09-09 — أنواع المنصات صارت بيانات seeder

- **أُزيلت إدارة أنواع المنصات من اللوحة بالكامل**: زر «إدارة الأنواع»، والصفحة المستقلة ومسارها، وإجراءات الإضافة والتعديل والحذف من المخزن. الأنواع تأتي من الباكند كبيانات seeder، فلا معنى لتحريرها من اللوحة.
- الأنواع ما زالت تُقرأ وتُعرض كما هي: badge النوع في جدول المنصات، وفلتر الفئات، وقائمة النوع في نموذج إضافة/تعديل منصة.
- مسار `/integrations/types` لم يعد موجوداً ويعيد التوجيه إلى لوحة التحكم.

## 2026-09-09 — إنشاء محادثة من الدردشة المباشرة

- **زر «محادثة جديدة»** في ترويسة قائمة المحادثات، يفتح نموذجاً لاسم الزائر وبريده (اختياري) والعميل التابع له والقناة (ويدجت / واتساب / بريد) وأول رسالة اختيارية.
- المحادثة تُنشأ بحالة **جديدة**، وتظهر في أعلى القائمة، وتُفتح مباشرةً بعد الإنشاء. أُضيف إجراء `createLiveChat` إلى المخزن؛ لم تكن هناك أي وسيلة لإنشاء محادثة من اللوحة من قبل.

## 2026-09-09 — بيان الباقة الحالية داخل نموذج الاشتراك

- **كل صف في قائمة العملاء يعرض باقته الحالية** (أو «بدون اشتراك»)، ويظهر معها اسم الباقة على زر الاختيار بعد اختيار العميل — فيتبيّن وضع العميل قبل المتابعة لا بعدها.
- **ملاحظة فورية عند الاختيار**: باختيار عميل له اشتراك ساري تظهر مباشرةً ملاحظة باسم باقته الحالية، وتتغيّر مع اختيار الباقة الجديدة حسب الحالة — **ترقية** أو **تخفيض** مع سؤال الاستبدال، أو تنبيه أنها **نفس الباقة الحالية**.
- **منع المتابعة على نفس الباقة**: اختيار الباقة التي يشترك بها العميل أصلاً يوقف المتابعة برسالة توضيحية بدل الدخول في مسار استبدال بلا معنى.
- شاشة تحذير الاستبدال بعد «متابعة» صارت تعكس السيناريو نفسه (ترقية / تخفيض) بدل عنوان واحد عام.

## 2026-09-09 — إصلاح اتجاه القوائم المنسدلة والمسافات

- **القوائم المنسدلة كانت تُرسم LTR**: Radix يضع `dir="ltr"` على المحتوى المنقول إلى portal ما لم يُحدَّد غير ذلك، فكانت الخيارات تُحاذى لليسار داخل صفحة RTL. ضُبط الاتجاه على مكوّن `SelectContent` نفسه، فأصبح الإصلاح شاملاً لكل القوائم في اللوحة لا لقائمة الباقات وحدها.
- **مسافات النموذج**: عناوين الحقول كانت `<label>` سطرية، فلم تُطبَّق المسافة بينها وبين عناصرها — ظهر ذلك بوضوح في «المدة» الملاصقة لأزرار شهري/سنوي. صارت العناوين على سطر مستقل بمسافة موحّدة مع باقي الحقول.

## 2026-09-09 — تنقيح نموذج إنشاء الاشتراك

- **اختيار العميل صار قائمة منسدلة** فيها حقل البحث بالداخل، بدل حقل بحث ظاهر دائماً تحته قائمة مفتوحة.
- **نُقل مفتاح الباقة التجريبية إلى نموذج إضافة العميل فقط** وأُزيل من نموذج إنشاء الاشتراك — التجربة تُمنح عند تسجيل العميل، فوجودها في المكانين كان ازدواجاً. كل اشتراك يُنشأ من هنا هو اشتراك مدفوع بحالة متأخر الدفع.
- **إصلاح نشر**: مسار `/integrations/types` لم يكن له مجلد على الخادم، فكان تحديث الصفحة أو فتحها برابط مباشر يعطي 404. أُنشئ المجلد بنفس نمط باقي المسارات.

## 2026-09-09 — تفعيل الباقة التجريبية عند إضافة العميل

- **مفتاح «تفعيل الباقة التجريبية»** في نموذج إضافة العميل، مفعّل افتراضياً. عند إطفائه يُسجَّل العميل بدون فترة تجريبية، فتبقى تجربته متاحة لاستخدامها لاحقاً عند إنشاء اشتراكه.
- **حالة عميل جديدة: «غير مفعّل»** للعميل المُسجَّل بلا تجربة ولا اشتراك — كانت كل العملاء الجدد تُسجَّل «تجريبي» حتى لو لم يبدأوا تجربة فعلياً. أُضيفت الحالة لكل عروض الحالة وللفلترة في صفحة العملاء.

## 2026-09-09 — إضافة عميل من داخل مسار إنشاء الاشتراك

- **زر «عميل جديد»** بجانب عنوان حقل اختيار العميل في نموذج إنشاء الاشتراك، يفتح نموذج العميل مباشرة، وبعد الحفظ يعود للنموذج والعميل الجديد مختار.
- **استُخرج نموذج العميل إلى مكوّن مشترك** (`ClientFormDialog`) تستخدمه صفحة العملاء ومسار إنشاء الاشتراك، بنفس الحقول والتحقق والآثار الجانبية، بدل تكرار النموذج في مكانين.
- ملاحظة سلوكية: العميل الجديد يُحفظ بحالة تجريبي لمدة 14 يوماً، فيُحتسب أنه استهلك تجربته ويظهر مفتاح «بدء بباقة تجريبية» معطّلاً عند إنشاء اشتراكه.

## 2026-09-09 — إنشاء اشتراك من صفحة الاشتراكات (US-136)

- **زر «إنشاء اشتراك»** في صفحة الاشتراكات يفتح نموذجاً لاختيار عميل موجود (بحث بالاسم أو البريد)، والباقة، ودورة الفوترة (شهري/سنوي). **لا يوجد حقل تاريخ بدء** — التاريخ يُحدَّد تلقائياً عند نجاح الدفع.
- **مفتاح «بدء بباقة تجريبية»** اختياري، ومتاح مرة واحدة فقط لكل عميل على مستوى النظام مهما كانت الباقة السابقة. إذا سبق للعميل استخدام تجربة، يظهر المفتاح معطّلاً مع تلميح يوضّح السبب.
- **إذا كان للعميل اشتراك ساري**، الضغط على «متابعة» يعرض تحذير الاستبدال، والتأكيد يدخل نفس مسار **تبديل الباقة** المستخدم من إجراءات الصف: تبديل حالاً (مع احتساب الفرق) أو عند انتهاء الباقة الحالية.
- **شاشة تأكيد نهائية** تعرض ملخّص العميل والباقة والمدة والسعر، وتاريخ البدء كقيمة معلّقة «يتحدد عند نجاح الدفع»، مع ملاحظة عن الحالة الناتجة.
- **الاشتراك يُنشأ بحالة متأخر الدفع** (أو تجريبي عند تفعيل المفتاح) ولا يصبح نشطاً أبداً قبل الدفع، ولا يُحتسب له إيراد حتى ذلك الحين. ويُسجَّل إشعار بأن العميل تلقّى إشعار الباقة الجديدة مع زر الانتقال للدفع.
- **توحيد مسار تبديل الباقة**: إجراء الصف كان يبدّل الباقة فوراً بمجرد الضغط عليها بدون خيار توقيت ولا تأكيد؛ صار يستخدم نفس مكوّن خيارات التوقيت المستخدم في مسار الإنشاء، فما عاد في اختلاف بين المدخلين.
- **إجراء «تأكيد استلام الدفع»** على الاشتراكات المتأخرة ينقلها إلى نشط ويضبط تاريخ البدء والفترة الحالية ويحتسب الإيراد — وهو المقابل الإداري لإتمام العميل للدفع.
- جدول الاشتراكات صار يعرض «يتحدد عند نجاح الدفع» بدل تاريخ وهمي، ويعرض الباقة المجدولة عند اختيار التبديل عند التجديد.

## 2026-08-13 — فورم العملاء

- **إرجاع اسم الشركة واسم المدير لحقول عادية** في فورم إضافة/تعديل العميل — الاسمان يُكتبان بلغة واحدة، فتابات اللغة كانت تضيف خطوة بلا فائدة. حُذف معها حقلا `companyNameAr` و`contactNameAr` من نوع العميل لأنهما لم يعودا مستخدمَين.

## 2026-08-13 — أنواع المنصات

- **صفحة مستقلة لإدارة أنواع المنصات** على `/integrations/types` بدل النافذة المنبثقة، فيها جدول CRUD كامل: الاسم بالعربية والإنجليزية، اللون، عدد المنصات المرتبطة، وأزرار التعديل والحذف.
- **فورم النوع** يستخدم تابات اللغة للاسم، مع اختيار لون من مجموعة مربعات ملوّنة.
- **اللون المختار يتحكم بلون badge النوع** في جدول منصات التكامل.
- **منع حذف نوع مرتبط بمنصات** مع رسالة توضّح عدد المنصات — كان الحذف سابقاً يفشل بصمت بدون أي رسالة.
- **المنصات وأنواعها انتقلت إلى المخزن المشترك** (`useAdminStore`)، فصارت الحالة تُحفظ بين الصفحات بدل أن ترجع للبيانات الأصلية مع كل تنقّل.
- تحسينات على الجدول: محاذاة عمود الاسم الإنجليزي مع اتجاه الصفحة RTL، إزالة عمود المعرّف الداخلي (`key`)، وعرض عيّنة لون بدل تكرار اسم النوع.

## 2026-08-12 — المحرّر الغني وحقول SEO

- **المحرّر الغني صار محرّراً واحداً بتابات** `العربية | En` بدل محرّرين متجاورين، مع تبديل اتجاه النص تلقائياً (RTL/LTR) وحالة تحرير وتراجع مستقلة لكل لغة.
- **حقول SEO**: من أربعة حقول (عنوان ووصف بلغتين) إلى **حقلين** بتابات، مع عدّاد حروف يتبع اللغة المفتوحة.
- **شريط التابات** استُخرج إلى مكوّن مشترك (`LanguageTabs`) تستخدمه كل الحقول ثنائية اللغة لضمان تطابق الشكل.

## 2026-08-11 — الإدخال ثنائي اللغة

- **مكوّن إدخال ثنائي اللغة** (`BilingualInput`): حقل واحد فوقه تابان `العربية | En` بدل حقلين متجاورين، طُبّق أولاً على فورم الخطة.
- **فورم العملاء**: اسم الشركة واسم جهة الاتصال صارا ثنائيَي اللغة.
- **تعميم النمط** على مجالات العمل، وتصنيفات قاعدة المعرفة، ومقالات المعرفة (العنوان والمحتوى وبيانات SEO).
- **إصلاح**: سجل النشاط كان يكسر البناء بسبب تسعة أنواع أحداث ناقصة — أُضيفت جميعها: تغيير البريد، تغيير كلمة المرور، جدولة إلغاء اشتراك، ترقية اشتراك، إعادة إرسال فاتورة، تسجيل موظف، ربط قناة، فصل قناة، شكوى مقدَّمة، ورد الدعم.

---

## ملاحظات قائمة

- **البيانات في الذاكرة فقط**: المخزن (zustand) بلا طبقة حفظ، فأي إضافة أو تعديل يزول مع إعادة تحميل الصفحة. يحتاج ربطاً بالـ API ليثبت.
- **ملفات زائدة في تطبيق العميل**: نشر خاطئ بتاريخ 2026-08-12 وضع ملفات بناء الأدمن داخل `qhub-client/assets-v2/`. أُعيد `index.html` و`version.json` لحالتهما الأصلية، وتبقّى حذف الملفات نفسها:

  ```bash
  cd /var/www/apexes.click/qhub-client/assets-v2 && \
  grep -rl 'index-oMQdhlZH' . | xargs -r rm -f && \
  rm -f index-oMQdhlZH.js index-BZgUEzeh.css ../cleanup-admin-junk.php
  ```

---

## آلية النشر

البناء يُرفع إلى `/var/www/apexes.click/qhub-admin/assets/`، ثم يُحدَّث `assets/boot.json` ليشير إلى ملفَي الـ JS والـ CSS الجديدين. الصفحة تقرأ `boot.json` بـ `no-store`، فتحديث عادي للصفحة يكفي لالتقاط النسخة الجديدة دون مسح ذاكرة التخزين المؤقت.
